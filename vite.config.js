import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset paths make the app work from GitHub Pages project URLs
  // such as /minecraft-v1/ as well as from localhost.
  base: './',
});
